package com.optimussoftware.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ADVERTISING_IMAGE".
*/
public class AdvertisingImageDao extends AbstractDao<AdvertisingImage, Long> {

    public static final String TABLENAME = "ADVERTISING_IMAGE";

    /**
     * Properties of entity AdvertisingImage.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Advertising_id = new Property(1, String.class, "advertising_id", false, "ADVERTISING_ID");
        public final static Property Image_id = new Property(2, String.class, "image_id", false, "IMAGE_ID");
    }


    public AdvertisingImageDao(DaoConfig config) {
        super(config);
    }
    
    public AdvertisingImageDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ADVERTISING_IMAGE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT UNIQUE ," + // 0: id
                "\"ADVERTISING_ID\" TEXT," + // 1: advertising_id
                "\"IMAGE_ID\" TEXT);"); // 2: image_id
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ADVERTISING_IMAGE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, AdvertisingImage entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String advertising_id = entity.getAdvertising_id();
        if (advertising_id != null) {
            stmt.bindString(2, advertising_id);
        }
 
        String image_id = entity.getImage_id();
        if (image_id != null) {
            stmt.bindString(3, image_id);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, AdvertisingImage entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String advertising_id = entity.getAdvertising_id();
        if (advertising_id != null) {
            stmt.bindString(2, advertising_id);
        }
 
        String image_id = entity.getImage_id();
        if (image_id != null) {
            stmt.bindString(3, image_id);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public AdvertisingImage readEntity(Cursor cursor, int offset) {
        AdvertisingImage entity = new AdvertisingImage( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // advertising_id
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // image_id
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, AdvertisingImage entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setAdvertising_id(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setImage_id(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(AdvertisingImage entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(AdvertisingImage entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(AdvertisingImage entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
